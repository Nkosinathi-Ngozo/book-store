<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <form action="/products?module=hello" method="get">
        <button type="submit">module</button>
    </form>
    <form action="/products?new=" method="get">
        <button type="submit">new</button>
    </form>
    <%for(let i = 0; i < data.images.length; i++){%>
        <a href="/products/find/<%=`${data.images[i].User.uuid}`%>/<%=`${data.images[i].uuid}`%>">
            <p>/uploadedImages/<%= data.images[i].img%></p>
            <p><%= `${data.images[i].uuid}`%></p>
            <p><%= `${data.images[i].User.uuid}`%></p>
            <img src="/uploadedImages/<%= data.images[i].img%>" alt="">
        </a>
    <%}%>
</body>
</html>