<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/css/singleproduct.css" type="text/css">
</head>
<body>
    <section>
        <div class="content">
            <div class="container">
                <img src="/uploadedImages/<%=product.img%>" alt="">
                <p><%=product.book_title%></p>
                <p><%=product.id%></p>
                <div class="cta">
                    <form action="/carts/<%= `${product.User.uuid}`%>/<%= `${product.uuid}`%>" method="post">
                        <button type="submit">add to cart</button>
                    </form>  
                </div>
            </div> 
        </div>
    </section>
</body>
</html>